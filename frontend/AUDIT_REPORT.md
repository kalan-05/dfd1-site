# Аудит FDC (2026-02-24)

## Исправлено

1. Убраны hardcoded SMTP-секреты из `public/php/mail_config.php`.
2. SMTP-конфиг переведен на переменные окружения, добавлены безопасные дефолты.
3. В `public/php/send-review.php` добавлена валидация обязательных SMTP-параметров.
4. `SMTP_DEBUG` отключен по умолчанию; лог SMTP включается только при явной настройке.
5. Хранилище логов/отзывов по умолчанию вынесено из `public` в `storage/*`.
6. Исправлен загрузчик Yandex Maps (`src/plugins/yandexMapsLoader.js`):
   - корректная сборка URL без API-ключа;
   - корректная обработка случая уже вставленного `<script>`.
7. `MapSection.vue` очищен от дублированного legacy-кода, переведен на общий загрузчик карт.
8. Роутинг исправлен: теперь есть реальный маршрут `NotFound` вместо редиректа всех 404 на `/`.
9. Исправлены SEO/infra-конфиги:
   - `public/.htaccess` (канонический домен, HTTPS, исправленные rewrite-правила);
   - `public/robots.txt`;
   - `public/sitemap.xml`.
10. Исправлен `public/favicon/site.webmanifest` (пути к иконкам и метаданные).
11. Добавлены `.gitignore` и `.env.example` для безопасной работы с репозиторием.
12. Обновлен `README.md` с актуальными шагами запуска и требованиями.
13. Исправлены контентные ошибки в `GallerySection.vue` (caption у карточек персонала).
14. Удален неиспользуемый код в `HeroSection.vue` и лишняя переменная в `Home.vue`.

## Проверки

- `npm run build` — успешно.
- `php -l` — не выполнен (в окружении отсутствует `php` CLI).

## Остаточные риски

1. Тяжелый ассет `doctors.webp` (~8.5MB) ухудшает LCP/скорость загрузки.
2. В проекте нет автоматических `lint/test`-скриптов.
3. В ряде Vue-файлов остается много закомментированного legacy-кода (поддержка и ревью сложнее).
